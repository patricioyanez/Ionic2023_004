import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'persistencia004',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
